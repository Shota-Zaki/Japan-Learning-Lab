# Java Learning Lab 作業停止

状態: 停止中  
停止日: 2026-08-06

FE Learning Labの完成判定が取り消されたため、Java Learning Labの設計・データ移行・画面接続・検証・デプロイは進めない。

再開条件:

1. FE Learning Labの科目A・科目Bが実装されている。
2. 複数条件絞り込みが検証済みである。
3. 問題本文、コード、表、画像、解説の構造化表示が検証済みである。
4. Repository直下`docs/`のGitHub Pages buildが成功している。
5. FEの保存、再開、履歴、復習、深いURL、レスポンシブ表示が回帰していない。
6. FEの独立レビューと公開確認が合格している。

停止前に追加されたJava基盤ファイルは削除せず保持するが、現在のアプリ入口と次作業指示からは切り離す。
