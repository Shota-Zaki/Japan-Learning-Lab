# Design Principles

この文書は、Japan Learning Lab全体のデザイン方針の正本とする。コース固有の詳細は、対象ディレクトリ内の設計文書へ記録する。

## 1. Product hierarchy

```text
Japan Learning Lab
└─ Engineer Learning Lab
   ├─ FE Learning Lab
   └─ Java Learning Lab
```

- 各階層にパンくずと戻り先を用意する
- 各学習サイトは、共通プラットフォームから遷移できる
- 各学習サイトは、固有URLから直接利用できる独立した入口を持つ
- 各学習サイトは、固有のローカルナビゲーションと視覚的アクセントを持つ
- 上位階層へ戻る経路を常に維持する

## 2. Core learning modes

各コースサイトは、次の2つを主要学習モードとする。

1. `レッスンで学ぶ`
2. `演習・模試で試す`

学習者が現在位置、次の操作、保存状態を判断できる情報を優先する。

## 3. Visual language

- 明るい面を中心にする
- 共通基調色は淡い水色とする
- 情報技術者試験向けサイトは緑系アクセントを使用する
- Java学習サイトは橙系アクセントを使用する
- 境界線は控えめにする
- 過度な影、グラデーション、装飾的な角丸を避ける
- 日本語の可読性を優先する
- 余白を十分に確保する
- 単純なリンク一覧ではなく、優先順位と次の行動が分かる構成にする

## 4. Content rules

- 学習時間、推定時間、経過時間は表示しない
- 保存データに存在しない進捗、苦手分野、履歴、正答率を捏造しない
- 保存データに存在しない技術的根拠、選択肢別理由、関連用語を捏造しない
- 設計理由や実装要件を学習者向け本文に表示しない
- 学習者向けの説明は、現在できることと次に行うことを中心にする
- 問題冊子や解答資料への外部リンクは、演習画面へ表示しない

## 5. Reference handling

外部の参考資料を使用する場合は、視覚的特徴を中立的な設計原則へ変換する。

参考元のサイト名、ブランド名、キャラクター名、ロゴ名、作品名、識別可能な固有表現を、次へ記載しない。

- UI文言
- README
- DESIGN.md
- コードとコメント
- CSSクラス名
- CSS変数名
- コンポーネント名
- ファイル名
- テスト名
- 差分ログ
- 引継ぎ文書

## 6. Responsive design

受入基準の代表幅:

- 375px
- 768px
- 1280px以上

共通要件:

- ページ全体の横スクロールを発生させない
- 表とコードは、必要な場合だけ各コンポーネント内部で横スクロールさせる
- タップ対象は原則44px以上を確保する
- 主要操作を画面幅ごとに見失わない
- 375pxでは1カラムを基本にする
- 768pxでは情報量に応じて1〜2カラムを使用する
- 1280px以上では本文と補助ナビゲーションの2カラムを利用できる

## 7. Accessibility

- 全操作をキーボードで実行できる
- アイコンのみの操作にはアクセシブルネームを付ける
- 開閉状態、選択状態、選択数を支援技術へ伝える
- 数値入力の許容範囲とエラーを支援技術へ伝える
- 色だけで状態を伝えない
- 見出し構造を維持する
- フォーカス位置を視認できる
- 画像には内容に対応した代替テキストを付ける
- 表は見出しセルとデータセルの関係を保持する

## 8. FE exercise information architecture

FE演習では、科目Aと科目Bを同一セッションへ混在させない。

開始前に次の順番で条件を決める。

1. 科目Aまたは科目B
2. 分野
3. 単元
4. 開催回または公開区分
5. 回答状態
6. 出題数または模擬試験種別

条件評価:

- 同一条件群の複数選択はOR
- 異なる条件群の組合せはAND
- 各条件群に全選択・全解除を配置
- 選択中条件をフィルター群より上に表示
- 個別解除と全解除を提供
- 0件の場合は開始操作を無効化

## 9. FE filter layout

- 絞り込みはコンパクトグリッド型を正式採用し、表示形式の切替は設けない
- 条件群を1〜3列のレスポンシブグリッドへ配置する
- 各ブロックは選択肢数に応じて個別に伸長し、同じ高さへ固定しない
- 条件群の内部に縦スクロールバーを設けず、全選択肢を常時表示する
- 長い項目名は省略記号で切らず、複数行へ折り返して全文を表示する
- 項目名と問題数を同じカード内で確認できるようにする
- 375pxでは1列、768pxでは最大2列、1280px以上では最大3列を基本とする
- 表示変更によって選択条件や抽出結果を変えない

## 10. FE question content

問題本文、選択肢、解説は、必要に応じて次のブロックへ分離する。

- `paragraph`
- `code`
- `table`
- `list`
- `note`
- `image`

表示要件:

- 本文、コード、表、選択肢、解説を視覚的に分離する
- コードは等幅書体と内部横スクロールを使用する
- 表は見出し、本文、列関係を保持する
- 図表は本文順序を維持して表示する
- 画像には`alt`を必須とする
- 解析できない内容を欠落させず、本文として保持する

## 11. FE question navigation

- 問題一覧の進捗表示と問題番号入力を近接配置する
- 問題番号は`1`から総問題数までの整数を受け付ける
- Enterと明示的な移動ボタンの両方を提供する
- 範囲外入力では問題を移動せず、許容範囲を通知する
- 問題番号ボタンの現在問題、回答済み、見直し状態を維持する
- 問題一覧は列数を画面幅へ適応させ、高さ上限を超えた場合だけ一覧内部を縦スクロールさせる
- 問題移動後は現在問題のボタンを一覧領域内で視認できる位置へ合わせる
- ページ全体の横スクロールを発生させない

## 12. FE detailed explanations

通常演習の回答確定後と完了後レビューは、同一の詳細解説構造を使用する。

表示順:

1. 正答
2. 正答の根拠
3. 選択肢ごとの判断
4. 関連知識

データ規則:

- 問題または選択肢に個別解説が保存されている場合は、それを優先する
- 個別解説がない場合は未登録であることを明示し、具体的な技術理由を生成しない
- 関連知識は保存済みの単元、キーワード、タグだけから構成する
- 正答根拠と選択肢本文を同時に確認できる配置にする
- 単一正答、複数正答、未回答を同じ表示規則で扱う
- 模擬試験中は従来どおり正誤と解説を表示しない

## 13. FE mock exams and completed review

- ランダム模擬試験と固定サンプル模擬試験を分離する
- 固定サンプルは科目ごとに開始する
- 科目Aは60問、科目Bは20問を維持する
- 公式問番号順を維持する
- 固定サンプルは並べ替え、抽出、ランダム化を行わない
- 問題、選択肢、正答、必要な図表が揃わない場合は開始させない
- 模擬試験終了前に正誤と解説を表示しない
- 完了後は各問題をキーボード操作可能な開閉ブロックとして表示する
- 完了後の詳細に問題文、選択肢、ユーザー回答、正答、正誤または未回答、詳細解説を表示する
- 単一正答、複数正答、未回答を同じ表示規則で扱う
- 履歴では通常演習、ランダム模擬試験、固定サンプル模擬試験を区別し、固定セット名を表示する

## 14. Design update workflow

UI変更時は、次の順番で進める。

1. Repositoryと現在の`DESIGN.md`を確認する
2. 変更が既存方針に含まれない場合、実装前に設計方針を更新する
3. 複数案で結果が大きく変わる場合だけ、実装前に案を提示する
4. 実装へ反映する
5. 375px、768px、1280px以上で検証する
6. アクセシビリティと横スクロールを確認する
7. 管理文書と検証記録を更新する

FE固有の詳細設計は`prototype/DESIGN.md`を参照する。競合時は、このRoot文書、最新ユーザー指示、GitHub実状態を優先する。
